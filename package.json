{
  "name": "@kumaya1109/nekoland-server-types",
  "version": "0.0.8",
  "description": "Typescript declarations for the nekoland lua script",
  "main": "src/index.d.ts",
  "author": "kumaya1109",
  "license": "MIT",
  "repository": "https://github.com/mogunogu/nekoland-server-types",
  "scripts": {
    "lint": "tslint -c tslint.json 'src/**/*.d.ts'",
    "build": "tsc -p tsconfig.json",
    "validate": "yarn run build && yarn run lint"
  },
  "devDependencies": {
    "tslint": "^5.18.0",
    "typescript": "^4.1.2"
  },
  "files": [
    "**/*.d.ts"
  ],
  "dependencies": {}
}